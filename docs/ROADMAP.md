# ROADMAP — Bounded Authority Report Adapter

ID / What / Acceptance / Depends / Why are **AUTHORED definitions** — hand-edited
by design, and they carry no live-state claims (no "currently", no pin shas, no
suite counts): frozen historical framing only. **Status and evidence are
DERIVED** from `.forge/`/`.kimosabe/` machine state — never hand-edited here.
(The forge-era deriver script was decommissioned from the harness home
2026-08-16; until a replacement lands, status is tracked by the alignment
audits and handoffs, and the rows below carry authored definitions only.)

Rows seed from the charter (`docs/charter.md`) + the strategy (`docs/strategy.md`).
The **Why** column links the governing section; a per-slice ADR
(`docs/adr/…`) supersedes the link when that slice lands.

Deferred scope routes to a backlog, never to tracker prose. The ID rides each
slice slug (`b2-ra-…`) as the deterministic join key.

<!-- forge-roadmap-schema: 1 -->

| ID | What | Acceptance | Depends | Why |
|---|---|---|---|---|
| RA1 | **Envelope sign** — the core signing API: take a report + a holder key handle, produce a grant + proof envelope via BAP's signing-input producers + `assemble_compact`. slug:b2-ra-envelope-sign | A report signed through the adapter verifies cleanly via `BoundedAuthorityProtocol.V1.check_envelope/2`; the round-trip is byte-compatible with the grant-holder-proof conformance vector; the holder key never leaves the caller (the adapter takes a key handle, not a path to a key) | — | [charter §2](charter.md) · [strategy §5](strategy.md) |
| RA2 | **Conformance round-trip** — a test harness making BAP's published vector the oracle: every published ENVELOPE case verifies via `check_envelope/2` and every GRANT-TIME case via `verify_grant/3`, plus an adapter-coherent round-trip (`sign_report/3` → `check_envelope/2`) against a freshly issuer-signed grant. slug:b2-ra-conformance-roundtrip | Every grant-holder-proof case in BAP's `priv/conformance/v1/vectors/` verifies green (via `check_envelope`/`verify_grant`); a defect-injected bad signature or tampered body goes red (non-vacuous); the harness re-runs against BAP's corpus at the pinned ref | RA1 | [charter §6](charter.md) · [strategy §7](strategy.md) |
| RA3 | **Dependency-direction proof** — a structural test asserting the adapter depends only on the public protocol package (no private runtime dep, no transport lib). slug:b2-ra-dep-direction | A test proves `mix.exs` declares `bounded_authority_protocol` and no `bounded_authority` runtime / no `replicant` / no `capstan`; a fixture adding a forbidden dep trips the gate red | — | [charter §6](charter.md) · [strategy §3](strategy.md) |
| RA4 | **Boundary-anchor signing** — `sign_anchor/3` signs a `BoundaryAnchor` via the shared companion-signer tail, returning a compact that round-trips through `verify_historical_anchor/3`. slug:b2-ra-anchor-signing | The library gains anchor signing as the second instantiation of its universal companion-signer role (ADR-0006); both key identifiers (`public_key`, `key_id`) come from the handle; wrong-key + exit/throw + defect-injection tripwires pass; signed anchor verifies green via `verify_historical_anchor`. The "checkpoint-ack" use case is a *consumer* of this capability (platform-side), not this library's scope. | RA1 | [charter §2](charter.md) · [ADR-0006](adr/0006-universal-companion-signer.md) |
| RA5 | **verifier application consumer change** — verifier application's report-signature plug accepts the `ba_protocol_v1` envelope (alongside S1's `eddsa_boundary_s1` during transition). slug:companion-signer-consumer | verifier application's `report_signature.ex` verifies the BA envelope via BAP's `check_envelope`; the `signature_scheme` column carries `ba_protocol_v1`; both schemes verify until S1's is retired; the verifier application-side dependency-direction wall stays green (verifier application depends on BAP, NOT on this adapter) | RA1, RA2 | [verifier application ADR 0007 §6](https://hexdocs.pm/bounded_authority_protocol) |
| RA6 | **Closeout** — the adapter's ADRs have landed (`docs/adr/0001`–`0006`, incl. the universal companion-signer scope); RA6 is the closeout gate: the verifier application ROADMAP B2 row derives SHIPPED and the full suite + conformance round-trip are green. slug:b2-ra-closeout | ADRs `docs/adr/0001`–`0006` landed; RA1–RA4 shipped; verifier application ROADMAP B2 row derives SHIPPED; full suite + conformance round-trip green | RA1, RA2, RA3, RA4, RA5 | [charter](charter.md) · [strategy](strategy.md) |
| RA7 | **Grant signing** (`sign_grant/3`) — the issuer-role extension. Building on the proof + boundary-anchor instantiations (RA1/RA4), this adds grant signing for issuer-side callers. Design-gated on the issuer-role reconciliation (ADR-0006's C1 pre-commitment: a holder key must never sign a grant; the `CountingKeyHandle` tripwire gates this red). slug:b2-ra-grant-signing | `sign_grant/3` signs a grant via the shared companion-signer tail; the holder-signs-grant tripwire stays RED (the C1 invariant); wrong-key + defect-injection tripwires pass; round-trip through `verify_grant/3` is green | RA1 | [ADR-0006](adr/0006-universal-companion-signer.md) §"The grant-signing pre-commitment" |
| RA8 | **Key-transition signing** (`sign_key_transition/3`) — the 4th named instantiation of the universal companion-signer role. Signs a key transition (the cryptographic handoff where the current retiring key asserts its successor) via the shared tail. slug:b2-ra-key-transition | `sign_key_transition/3` signs a `KeyTransition` via the shared tail; round-trips through `verify_key_transition/4`; wrong-key + defect-injection tripwires pass. **Role-agnostic** (mirrors `sign_anchor/3`, not `sign_grant/3`) — see ADR-0009. | RA1 | [ADR-0006](adr/0006-universal-companion-signer.md) · [ADR-0009](adr/0009-key-transition-signing.md) |
| RA9 | **Edge-agent reference implementation** — a minimal runnable Elixir app (`examples/edge_agent/`) that imports the adapter, holds a holder key (config-driven), signs reports, and POSTs them to a receiver. The proof that the adapter works in a real deployment + the thing the Agent Studio provisions. slug:b2-ra-edge-agent-ref | The app reads a grant + key from config; calls `sign_report/3`; POSTs `{grant, proof, report}` to a configurable receiver URL; the receiver verifies via `check_envelope`; a README explains how to run it | RA1, RA2 | [consumer-integration.md](../docs/consumer-integration.md) |
| RA10 | **Second-language verifier** (TypeScript or Python) — **NARROWED: out of BARA's scope.** A cross-language verifier validates `bounded_authority_protocol`'s *portable format* — a deliverable BAP [ADR-0014](https://github.com/baselabs/bounded_authority_protocol/blob/main/docs/adr/0014-cross-language-verifier-sdks.md) `cross-language-verifier-sdks` (accepted; ratifies ROADMAP BAP-09: TypeScript + Python verifier SDKs consuming the spec + the [ADR-0005](https://github.com/baselabs/bounded_authority_protocol/blob/main/docs/adr/0005-portable-conformance-corpus-and-verifier-cli.md) conformance corpus) owns as a BAP surface, not a BARA one. BARA is the Elixir companion-signer library; the verifier SDK is a property of the protocol package the verifier consumes, so the deliverable belongs to BAP. The row stays on BARA's board only to record the redirect + keep the `b2-ra-cross-language-verifier` join key resolvable. slug:b2-ra-cross-language-verifier | _(Not a BARA deliverable — tracked as a redirect.)_ A cross-language verifier would pass every published conformance case (positive + negative); the conformance round-trip would be green in a second language. Owned in BAP (ADR-0014). | RA2 | [BAP ADR-0014](https://github.com/baselabs/bounded_authority_protocol/blob/main/docs/adr/0014-cross-language-verifier-sdks.md) (cross-language verifier SDKs) · [BAP ADR-0005](https://github.com/baselabs/bounded_authority_protocol/blob/main/docs/adr/0005-portable-conformance-corpus-and-verifier-cli.md) (the conformance corpus the SDKs consume) · [commercial-strategy](https://github.com/baselabs/bounded_authority/blob/master/docs/strategy/commercial-strategy.md) §3.2 |
| RA11 | **Role-attestation consumption** — `sign_grant/3` accepts a BA-signed `RoleAttestation` compact as an input (the same input class as `grant_compact`), calls `BAP.verify_attestation/2`, and gates on the attested role == `:issuer` + the attested `public_key` matching the handle's. Strengthens the C1 gate (ADR-0007 §Decision 5) from a handle self-declaration to a BA-asserted, cryptographically-signed role binding. slug:b2-ra-role-attestation | `sign_grant/3` resolves a BA-signed role-attestation compact (caller-supplied input); `BAP.verify_attestation/2` confirms the BA signature (against a configured BA trust-root) + the role + the key binding; a holder/roleless/lying-handle key is rejected at the BA-attestation layer, not just the declaration layer; wrong-key + defect-injection tripwires pass; the BARA dep wall holds (depends only on BAP; the attestation crosses as data) | **BAP** (define `RoleAttestation` + `verify_attestation/2`), **BA** (issue role-attestations at key provisioning — BA's attestation-service direction). Cross-repo: this row tracks BARA's piece; the BAP + BA pieces are owned in their repos. | [ADR-0008](adr/0008-role-attestation-direction.md) · [ADR-0007](adr/0007-grant-signing.md) §Decision 5 (the C1 declaration-rejection residual) · [charter §4](charter.md) |
