/_build/
/deps/
/doc/
/cover/
/tmp/
erl_crash.dump
*.ez
bounded_authority_report_adapter-*.tar

/.elixir_ls/
/.lexical/
/.serena/
/graphify-out/
/.env
/.env.*

# Forge lifecycle artifacts are local; the surface declarations (critical + conformance) are tracked.
/.forge/*
!/.forge/critical-surfaces
!/.forge/conformance-surfaces
